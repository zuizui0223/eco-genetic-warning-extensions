# Branching-headroom verification

The exact theorem, implementation and tests were added together. This marker exists only to force a pull-request validation pass over the current repository state before the headroom result is promoted into the flagship manuscript.

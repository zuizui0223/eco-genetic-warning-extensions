# Phase V execution scope

Only the committed preregistration in `docs/CROSS_LAYER_ALIGNMENT_PHASE_V_PREREGISTRATION.json` governs the scientific run.

The literature bridge motivates the state variable tested but does not authorize parameter changes. The workflow output may support either a downstream incidence effect or a bounded null result. Neither outcome opens an automatic parameter sweep.

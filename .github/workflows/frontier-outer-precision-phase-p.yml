name: Frontier outer precision validation Phase P

on:
  pull_request:
    branches:
      - "agent/headline-r3-validity-phase-l"
    paths:
      - "src/eco_genetic_warning_extensions/frontier_outer_precision_phase_p.py"
      - "src/eco_genetic_warning_extensions/frontier_outer_precision_phase_p_runner.py"
      - "scripts/run_frontier_outer_precision_phase_p_seed.py"
      - "scripts/aggregate_frontier_outer_precision_phase_p.py"
      - "tests/test_frontier_outer_precision_phase_p.py"
      - "docs/FRONTIER_OUTER_PRECISION_PHASE_P.md"
      - ".github/workflows/frontier-outer-precision-phase-p.yml"
  workflow_dispatch:
permissions:
  contents: read
jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: |
          python -m pip install --upgrade pip pytest
          python -m pip install -e .
          python -m pytest -q tests/test_frontier_outer_precision_phase_p.py
  seed-block:
    needs: contract
    strategy:
      fail-fast: false
      matrix:
        master_seed: [20290320,20290321,20290322,20290323,20290324]
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
        with: {path: extension}
      - uses: actions/checkout@v4
        with:
          repository: zuizui0223/eco-genetic-criticality
          ref: dd8ee379d0d3518194c767d16402042525bc00dc
          path: upstream
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: |
          python -m pip install --upgrade pip
          python -m pip install -e upstream
          python -m pip install -e extension
      - working-directory: extension
        run: |
          mkdir -p artifacts/frontier_outer_precision
          python scripts/run_frontier_outer_precision_phase_p_seed.py --upstream-checkout ../upstream --master-seed ${{ matrix.master_seed }} --output artifacts/frontier_outer_precision/seed_${{ matrix.master_seed }}.json
      - uses: actions/upload-artifact@v4
        with:
          name: phase-p-seed-${{ matrix.master_seed }}
          path: extension/artifacts/frontier_outer_precision/seed_${{ matrix.master_seed }}.json
          if-no-files-found: error
          retention-days: 90
  aggregate:
    needs: seed-block
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: "3.11"}
      - run: python -m pip install -e .
      - uses: actions/download-artifact@v4
        with:
          pattern: phase-p-seed-*
          path: artifacts/frontier_outer_precision/seeds
          merge-multiple: true
      - run: python scripts/aggregate_frontier_outer_precision_phase_p.py --input-glob 'artifacts/frontier_outer_precision/seeds/seed_*.json' --output artifacts/frontier_outer_precision/phase_p_summary.json
      - uses: actions/upload-artifact@v4
        with:
          name: frontier-outer-precision-phase-p
          path: artifacts/frontier_outer_precision/phase_p_summary.json
          if-no-files-found: error
          retention-days: 90
